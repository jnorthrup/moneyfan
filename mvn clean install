
This will compile both modules and run tests.

## Future Directions (Conceptual)

*   Refine `bbcursive` to support `TypedParser<T>` more natively for direct typed-value extraction.
*   Implement a fully `bbcursive`-native CSV parser within `bikeshed`.
*   Further optimize ISAM read/write paths using advanced `bbcursive` techniques.
*   Explore LLM interaction by exposing `bbcursive` grammars and `bikeshed` schemas.

## Contributing

(Details on contributing, coding standards, etc., if applicable)
