Craft a comprehensive Maven Java project prompt for a Domain-Specific Embedded Language (DSEL) designed as a cursor-based replacement for Pandas, emphasizing immutable `Join<F, S>` 2-tuples and compositional operations. This DSEL, built on two Maven modules, `bbcursive` and `bikeshed`, will leverage Kotlin-inspired meta-programming adapted to Java idioms, particularly through aggressive code condensation into enums and the pervasive use of glyph-based shorthands optimized for IntelliJ's code completion. The foundational `bbcursive` module will provide a minimal, zero-copy `ByteBuffer` parsing combinator library, directly supporting `bikeshed`'s efficient data handling. `bikeshed`, the core DSEL module, will define primitives like `Series<T>` (implemented as `Join<Integer, java.util.function.IntFunction<T>>` for efficient, functional data access), `RowVec` (a `Series` of `Join<Object, java.util.function.Supplier<ColumnMeta>>` instances for row-vector abstraction), and `Cursor` (a `Series` of `RowVec` instances, representing the core tabular data structure). All DSEL operations must strictly adhere to immutability, returning new `Join` instances rather than modifying existing ones, thus promoting compositional purity and limiting uncontrolled lambda captures. User interaction with the DSEL should exclusively occur through these `Join`-based interfaces. Centralize DSEL operations within an omnibus enum, `D`, which will contain core functions (e.g., `D.jn` for `Join` creation, `D.swap` for `Join` inversion, `D.alpha` or `D.map` for transformations) and type-safe utilities. Extend the `TypeMemento` interface to align with ISAM fixed-size data by integrating `networkSize` properties into its `IOMemento` enum implementation, facilitating direct memory-mapped access patterns for `Series` data, mirroring how `mmap` would be used for large time-series datasets. Implement `Twin<T>` as a specific `Join<T, T>` to enforce symmetric pairs. For the trading simulator gap analysis, clearly articulate how this DSEL supports: (1) data ingestion from Binance archives to ISAM, ensuring efficient time-series cursoring via `IsamDataFile` and `CSVUtil` leveraging `bbcursive`'s zero-copy parsing; (2) concurrent trade agents operating on a shared, immutable timeline, implicitly supported by the DSEL's immutable `Join`-based structures and potential integration with a non-blocking `Reactor` pattern for `SelectableChannel` events; and (3) reward-based agent visibility rules, which can be expressed through Join-based `Predicate` primitives and composed into complex logical filters. Mandate strict coding conventions: (a) group related functions and constants within enums (e.g., `D`, `IOMemento`); (b) employ lambdas *only* when capturing `Join` instances (or their subtypes), preferring concise function references where applicable (e.g., `Series::get`, `TypeMemento::networkSize`); (c) leverage ternary operators (`condition ? ifTrue : ifFalse`) for conditional logic brevity; and (d) ensure that the provided `pom.xml` correctly establishes the project's multi-module structure, with `bikeshed` properly declaring a dependency on `bbcursive`, and `dsel-bbcursive-parent` serving as the parent POM for both, managing common dependencies like JetBrains annotations and JUnit 5.
