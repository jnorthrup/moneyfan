package com.moneyfan.dsel.core;

/**
 * Conceptual type alias for Join&lt;String, TypeMemento&gt;.
 * Represents metadata for a column, typically its name and type.
 * <p>
 * This is not a concrete class but a conceptual type.
 * Instances are created using {@link Types#cm(String, TypeMemento)}.
 */
public interface ColumnMeta {
}
